# Antigravity Rules & Guidelines

## 開発フロー (Development Workflow)

- **Pull Request (PR) 必須**: タスク実行時は直接 `master` ブランチに変更をPushせず、必ず新しい作業用ブランチを作成し、そこにPushした上でPull Request (PR) を作成すること。これにより、ユーザーがコードレビューを行えるようにする。
